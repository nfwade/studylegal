/*$(document).ready(function(){
  $('.ui.basic.modal.cancel-modal')
  .modal({
    closable  : false,
    onDeny    : function(){
      window.alert('Wait not yet!');
      return false;
    },
    onApprove : function() {
      $('#cancelaccountform').submit();
    }
  });
  $('#cancelaccountform').submit(function(event){
    $('#cancel-modal').modal('show');
    event.preventDefault();
  });
});
*/
